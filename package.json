{
  "name": "action-term-on-fail",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "ncc build src/main.js -o dist && ncc build src/post.js -o dist/post && mv dist/post/index.js dist/post.js && rm -rf dist/post",
    "build:main": "ncc build src/main.js -o dist",
    "build:post": "ncc build src/post.js -o dist/post-tmp && mv dist/post-tmp/index.js dist/post.js && rm -rf dist/post-tmp"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "otpauth": "^9.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
